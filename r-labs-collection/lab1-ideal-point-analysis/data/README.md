Оригинальный файл данных был привязан к варианту задания (вариант 26) и в репозиторий не входит. Файл, который лежит здесь, синтетический: его создаёт `generate_data.R` с той же структурой столбцов, поэтому основной скрипт работает от начала до конца. Цифры в результатах из-за этого отличаются от цифр в отчёте

Если файла нет, основной скрипт сам запустит генератор
